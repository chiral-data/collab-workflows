# Node 4: Prepare ligands (SDF → PDBQT)
depends_on = ["02_LigandInput"]
inputs = ["ligands/*.sdf"]
outputs = ["ligands_prepared/*.pdbqt"]

[container]
docker_image = "workflow-esraa/node4:latest"

[scripts]
