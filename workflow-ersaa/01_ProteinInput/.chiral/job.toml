[job]
name = "download_receptor"
description = "Download receptor PDB structure from RCSB."

[inputs]
protein_id = { type = "string", required = true }

[outputs]
receptor_pdb = { type = "file", path = "receptor.pdb" }

[container]
dockerfile = "Dockerfile"

[scripts]
run = "run.sh"
