depends_on = ["07_Docking"]

[inputs]
vina_results = { type = "folder", required = true }

[outputs]
ranked_excel = { type = "file", path = "results.xlsx" }

[container]
dockerfile = "Dockerfile"

[scripts]
