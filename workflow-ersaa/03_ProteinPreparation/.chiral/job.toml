# Node 3: Prepare receptor (PDB → PDBQT)
depends_on = ["01_ProteinInput"]
inputs = ["receptor.pdb"]
outputs = ["receptor.pdbqt", "protein_fixed.pdb"]

[container]
docker_image = "workflow-esraa/node3:latest"

[scripts]
