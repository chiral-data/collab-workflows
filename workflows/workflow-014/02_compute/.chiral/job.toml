name = "Compute ADMET Predictions"
description = "Runs ADMET-AI prediction engine on validated molecules"

inputs = ["standardized_molecules.csv"]
outputs = ["outputs/raw_predictions.csv"]

[container]
image = "continuumio/miniconda3"

[scripts]
pre = "pre_run.sh"
run = "run.sh"
