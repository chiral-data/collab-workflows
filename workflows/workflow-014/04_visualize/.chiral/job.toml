name = "Generate Report"
description = "Generates a self-contained HTML dashboard with radar charts and data tables"

inputs = ["filtered_candidates.csv"]
outputs = ["outputs/report.html"]

[container]
image = "python:3.11-slim"

[scripts]
run = "run.sh"
