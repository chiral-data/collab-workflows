name = "Validate Inputs"
description = "Validates the input CSV and standardizes SMILES strings"

inputs = ["*.csv"]
outputs = ["outputs/validated_molecules.csv", "outputs/validation_report.json"]

[container]
image = "python:3.11-slim"

[scripts]
pre = "pre_run.sh"
run = "run.sh"
