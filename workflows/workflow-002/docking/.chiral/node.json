{
  "name": "docking",
  "description": "Prepares receptor structure in PDBQT format and performs in-silico screening using SMINA docking for all ligands in the ligand library.",
  "params": {
    "exhaustiveness": ["integer", 8, "smina config"],
    "num_modes": ["integer", 1, "smina config"],
    "energy_range": ["integer", 3, "smina config"]
  }
}

