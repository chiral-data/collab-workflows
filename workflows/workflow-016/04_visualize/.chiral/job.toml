name = "Generate Docking Report"
description = "Generates a self-contained HTML report with 3D visualization and ranked docking pose table"

inputs = ["top_predictions.pdb", "rank_results.json"]
outputs = ["report.html"]

[container]
image = "lightdock-pipeline:latest"

[scripts]
run = "run.sh"
