name = "Preprocessing"
description = "Convert validated input to Boltz-2 (YAML) and Chai-1 (FASTA) formats"
inputs = ["validated_input.yaml"]
outputs = ["boltz_input.yaml", "chai_input.fasta"]

[container]
image = "ghcr.io/chiral-data/boltz:2025_09_05"
use_gpu = false

[scripts]
run = "run.sh"
