name = "Data Preparation"
description = "Ingest raw SMILES data, compute physicochemical descriptors, and generate MACCS keys."
outputs = ["outputs/data.json", "outputs/report.html", "outputs/descriptors.csv"]

[container]
image = "chiral.sakuracr.jp/qsar:20260107_v1"

[scripts]
run = "run.sh"