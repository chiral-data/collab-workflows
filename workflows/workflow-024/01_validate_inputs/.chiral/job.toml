name = "Validate Inputs"
description = "Validate ligand file format; fetch receptor and co-crystal ligand from RCSB PDB; confirm resolution < 3 A"

outputs = ["receptor.pdb", "native_ligand.sdf", "validated_ligands.smiles", "validation_report.json"]

[container]
image = "vina-gnina-pipeline:latest"
use_gpu = false

[scripts]
run = "run.sh"
